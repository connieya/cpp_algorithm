#include "bits/stdc++.h"

using namespace std;

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    bool visited[11] = {false,};

    for (auto v: visited) {
        cout << v << ' ';
    }
    cout << '\n';
}