#include "bits/stdc++.h"

using namespace std;

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    vector<vector<int>> edges;
    int len = edges.size();
    for (int i = 0; i < len; ++i) {
        for (int j = 0; j < edges[i].size(); ++j) {

        }
    }
}