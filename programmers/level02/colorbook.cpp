#include "bits/stdc++.h"

using namespace std;

int dx[] = {-1, 0, 1, 0};
int dy[] = {0, 1, 0, -1};

bool visited[100][100];

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    queue<pair<int, int>> Q;
    while (!Q.empty()) {
        auto cur = Q.front();
    }
    fill(visited[0],visited[0]+10, false);

}