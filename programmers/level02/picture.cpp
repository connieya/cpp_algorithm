#include "bits/stdc++.h"

using namespace std;

bool isValid(string str , vector<char> friends) {

}

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    vector<char> friends = {'A','C','F','J','M','N','R','T'};
    int cnt =0;
    do {

    } while (next_permutation(friends.begin(),friends.end()));
    cout << cnt << '\n';
    friends.size();
    cout << friends[1] << '\n';
}