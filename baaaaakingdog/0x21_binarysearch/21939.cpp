#include "bits/stdc++.h"

using namespace std;

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    multiset<pair<int , int>> ms;
    int n;
    cin >> n;
    while (n-- ){
        int num , rank;
        cin >> num >> rank;
        ms.insert({rank,num});
    }
    cin >> n;
    while (n-- ){
        string command;
        int v;
        cin >> command;

    }
}