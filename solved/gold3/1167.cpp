#include "bits/stdc++.h"
#define MAX 100001

using namespace std;

vector<pair<int,int>> tree[MAX];

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    int n;
    cin >> n;
    for (int i = 0; i < n; ++i) {
        int a,b,node,cnt=0;
        cin >> a;
        while (1){
            cin >> b;
            if (b==-1) break;
            node = b;

        }
    }

}