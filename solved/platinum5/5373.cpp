#include "bits/stdc++.h"

using namespace std;

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    int t,cnt;
    string str;
    cin >> t;
    while (t--) {
        cin >> cnt;
        for (int i = 0; i < cnt; ++i) {
            cin >> str;
        }
    }
}